# Datrix Web

The Datrix web application provides the workspace for multi-agent data analysis. It is built with Next.js and exported as static assets for the backend to serve.

```bash
npm install
npm run compile
```

The export is written to `web/out` and published by `scripts/start_dataman_services.sh`.
